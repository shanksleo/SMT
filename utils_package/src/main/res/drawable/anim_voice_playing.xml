<?xml version="1.0" encoding="utf-8"?>
<animation-list xmlns:android="http://schemas.android.com/apk/res/android"
    android:oneshot="false">
    <item
        android:drawable="@drawable/message_voice_playing_1"
        android:duration="150" />
    <item
        android:drawable="@drawable/message_voice_playing_2"
        android:duration="150" />
    <item
        android:drawable="@drawable/message_voice_playing_3"
        android:duration="150" />
</animation-list>


    <!--
        简单的动画实现
        ivVoiceIcon.setBackgroundResource(R.drawable.anim_voice_playing);
            voiceAnim = (AnimationDrawable) ivVoiceIcon.getBackground();
            if (voiceAnim != null) {
                voiceAnim.start();
            }

        if (voiceAnim != null && voiceAnim.isRunning()) {
                        voiceAnim.stop();
                    }
    -->