<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.open.fire.pic">
    <!--修改第三方库的最低兼容版本-->
    <uses-sdk tools:overrideLibrary="org.qcode.qskinloader,com.facebook.shimmer,com.tencent.rtmp.liteavsdk,com.libksysv_java,com.libksysv_armv7a ,com.hpplay.sdk.source.api,com.tencent.matrix.iocanary,com.tencent.sqlitelint,com.tencent.matrix.android.commoms,com.tencent.matrix.trace,com.tencent.matrix.resource,com.tencent.matrix,com.zk.qpm" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:name="com.jess.arms.base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 只要依赖 AutoSize 就必须填写设计图尺寸, 否则报错, 不想使用 AutoSize 就不要依赖 AutoSize
       只要填写完设计图的尺寸, AutoSize 就会自动启动, 以下 dp 尺寸是根据公式 px / (dpi / 160) 求出, 运算时使用测试机的 dpi 即可
       AutoSize 的详细介绍请看这里 https://juejin.im/post/5bce688e6fb9a05cf715d1c2
       -->
        <meta-data
            android:name="design_width_in_dp"
            android:value="360" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="640" />


        <activity
            android:name=".mvp.ui.activity.SplashActivity"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".mvp.ui.activity.HomeActivity"
            android:exported="true"
            android:launchMode="singleTask">
        <!--android:exported="true" 在Activity中该属性用来标示：当前Activity是否可以被另一个Application的组件启动：true允许被启动；false不允许被启动。-->
        </activity>
        <activity android:name=".mvp.ui.activity.DaaggerActivity" />
        <activity android:name=".mvp.ui.activity.ConfigActivity"
            android:configChanges="keyboardHidden"
            />

        <!-- Arms 配置 -->
        <meta-data
            android:name="com.open.fire.pic.app.PicConfig"
            android:value="ConfigModule" />
        <receiver android:name=".alarm.LoongggAlarmReceiver"
            android:process=":alarm"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.shanks.alarm"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>